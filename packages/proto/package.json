{
  "name": "@ibex-harness/proto",
  "private": true,
  "version": "0.0.0",
  "description": "Workspace deps for local TypeScript stubs from buf generate (gen/ is gitignored).",
  "dependencies": {
    "@bufbuild/protobuf": "^2.14.0"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  }
}
