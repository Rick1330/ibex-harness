coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%
    patch:
      default:
        target: 95%
  ignore:
    - "packages/proto/gen/go/**"
    # Playwright/E2E harness binaries (not production code).
    - "services/**/cmd/**/*-fixture/**"
  flags:
    go:
      paths:
        - packages/
        - services/
    embedder:
      paths:
        - services/embedder/app/
    context:
      paths:
        - services/context/app/
