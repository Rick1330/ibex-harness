# golang.org/x/mod tooling CVEs (fixed in 0.40.0). Tracked for a follow-up
# bump of .github/tools modules outside docs PRs; Trivy SARIF mode currently
# applies exit-code without the CRITICAL,HIGH severity filter.
# Revisit by 2026-12-01 when bumping CI tool modules.
CVE-2026-56864
CVE-2026-56865

# image-size DoS (no patched release <=2.0.2). Same acceptance as osv-scanner.toml:
# transitive via fumadocs-core; docs build does not feed untrusted buffers into
# image-size. Revisit by 2026-12-01.
CVE-2025-71329
CVE-2025-71330
