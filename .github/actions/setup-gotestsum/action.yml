name: Setup gotestsum
description: Install gotestsum for JUnit test reporting

runs:
  using: composite
  steps:
    - name: Install gotestsum
      shell: bash
      run: bash .github/scripts/install-go-ci-tool.sh gotest.tools/gotestsum
