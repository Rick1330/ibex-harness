# Staging overrides — digests filled by CI after docker-publish.
hpa:
  minReplicas: 2
  maxReplicas: 6
rollout:
  progressDeadlineSeconds: 300
backup:
  s3Endpoint: "https://minio.staging.example"  # swap to real object storage
