route:
  receiver: local-null
  group_by: [alertname]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 4h

receivers:
  - name: local-null
    # Local-only sink; wire webhooks in real environments.
