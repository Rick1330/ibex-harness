# Billing redesign

First viewport is spend only: period, amount vs cap, progress, full-width
burn chart, and a compact enforcement control — no side panel, no plan card,
no mono field names in the hero.

Plan, rates, usage, and decisions live in tabs so the page is not a wall of
equal-weight sections. Honesty gates unchanged (estimated-only, hard cap →
402, unavailable ≠ deny, five usage shapes).
