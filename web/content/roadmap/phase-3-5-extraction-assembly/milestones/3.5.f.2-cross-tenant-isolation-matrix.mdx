---
title: "3.5.F.2 Cross-tenant isolation matrix"
description: "Four new ISO-MCP-* cases covering MCP-specific tenant isolation: cross-org agent access, suspended agent handling, and extraction worker RLS enforcement."
summary: "Four new ISO-MCP-* cases covering MCP-specific tenant isolation: cross-org agent access, suspended agent handling, and extraction worker RLS enforcement."
status: "completed"
milestoneId: "3.5.F.2"
track: "F"
trackTitle: "Exit Gate"
trackHref: "/roadmap/phase-3-5-extraction-assembly/tracks#track-f--exit-gate"
goal: "[Track F — Exit Gate](/roadmap/phase-3-5-extraction-assembly/tracks#track-f--exit-gate)"
estimatedEffort: "2 days"
phase: "phase-3-5-extraction-assembly"
fullTitle: "Milestone 3.5.F.2 — Cross-Tenant Isolation Matrix"
---

# Milestone 3.5.F.2 — Cross-Tenant Isolation Matrix

**Status:** Completed  
**Goal:** [Track F — Exit Gate](/roadmap/phase-3-5-extraction-assembly/tracks#track-f--exit-gate)  
**Phase:** 3.5 — Extraction & Context Assembly  
**Estimated effort:** 2 days  
**Track:** [Track F — Exit Gate](/roadmap/phase-3-5-extraction-assembly/tracks#track-f--exit-gate)  
**Depends on:** Milestone 3.5.F.1 (E2E suite)

---

<Callout type="note" title="Planning baseline">
  This page started as a planning sketch. Named paths and checklists were orientation
  aids. The landed shape below reflects live code constraints (MCP-only ValidateAgent
  wiring; ISO-MCP-04 MockTransport for memory HTTP; E.2 bearer-forward test de-collided
  from the ISO-MCP-01 ID).
</Callout>

<Callout type="info" title="Sketch deviations">
  - **MCP ValidateAgent:** `packages/authclient` encodes ValidateAgent; mcp-memory
    calls Auth `ValidateAgent` (org + active) after `resolve_tool_agent_id` and before
    memory HTTP. No proxy / proto / Go `security-integration` changes.
  - **ISO-MCP-04:** Real Postgres session store + RLS GUC; memory HTTP captured with
    `httpx.MockTransport` (assert zero POSTs). Provider is a stub.
  - **E.2 rename:** Bearer-forward unit test docstring no longer claims `ISO-MCP-01`
    so the isolation matrix owns that ID.
</Callout>

## Test Matrix

| Case ID | Scenario | Expected | Environment |
|---|---|---|---|
| ISO-MCP-01 | Org A's PAT calls `search_memory` **and** `write_memory` with Org B's `agent_id` | `permission_denied` MCP error, never `not_found` | Live Postgres + auth + memory HTTP |
| ISO-MCP-02 | Org A's org-scoped PAT + Org A agent `created_by` User B | Allowed (`isError` false / memory 200) | Live Postgres + auth + memory HTTP |
| ISO-MCP-03 | Agent-scoped PAT for `suspended` agent on search + write | `permission_denied` via ValidateAgent | Live Postgres + auth + memory HTTP |
| ISO-MCP-04 | Extraction with forged `org_id=OrgA`, `session_id=OrgB` | `session_not_found`; zero memory POSTs; pointer unchanged | Live Postgres + MockTransport HTTP |

---

## Orientation (indicative)

**Areas involved:**

- MCP server (`services/mcp-memory`) — `GRPCAgentVerifier` + tool call site
- Authclient ValidateAgent codec
- Worker extraction session store (ISO-MCP-04)
- CI job `security-integration-p35` (`infra/scripts/security-integration-p35.sh`)

## Success signals

- [x] All 4 ISO-MCP-* cases automated (min-count floor 4 in CI)
- [x] CI job `security-integration-p35` enforces minimum test count
- [x] Repo guards / CI checks still pass

## Prerequisites

- Milestone 3.5.F.1 (E2E suite)
- Milestone 3.5.E.2 (MCP tools)
